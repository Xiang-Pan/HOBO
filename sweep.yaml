program: grid_search.py
method: grid
metric:
  name: loss
  goal: minimize
parameters:
  parameter_name:
    distribution: categorical
    values:
    - elu
    - celu
    - gelu
    - selu
    - relu
    - prelu
    - lrelu
    - rrelu
    - relu6
  # learning_rate:
  #   min: 0.0001
  #   max: 0.1
  # optimizer:
  #   values: ["adam", "sgd"]